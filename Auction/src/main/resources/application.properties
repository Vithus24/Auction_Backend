# =========================
# RDS MySQL (direct)
# =========================
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://auctionmysql.c9kq0e6uwkrg.ap-south-1.rds.amazonaws.com:3306/auction?createDatabaseIfNotExist=true&sslMode=REQUIRED&allowPublicKeyRetrieval=true
spring.datasource.username=admin
spring.datasource.password=Vithus24

# Connection pool (Hikari)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.validation-timeout=5000

# JPA / Hibernate
spring.jpa.show-sql=false
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Server
server.port=8080

# JWT (hardcoded only because you asked)
jwt.secret=a196b847b80d980c811daaed23bf089d9e3c2023531677900b2508b7ef853654
jwt.expiration=86400000

# Logging
logging.level.org.springframework=INFO
logging.level.org.springframework.security=INFO
logging.level.Auction.Auction=INFO

# Redis (only if you actually have Redis on the EC2)
spring.data.redis.host=auction-redis-f2m5bb.serverless.ap-south-1.cache.amazonaws.com
spring.data.redis.port=6379
#spring.data.redis.ssl=true
spring.data.redis.password=null
spring.data.redis.timeout=3s
spring.data.redis.connect-timeout=3s

# Email (Gmail example)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=moratechiegiant@gmail.com
spring.mail.password=iqan qjap full pcnw
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Verification code
verification.code.length=6
verification.code.expiry=300

# Uploads
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=500MB

auction.timer.timeout=10